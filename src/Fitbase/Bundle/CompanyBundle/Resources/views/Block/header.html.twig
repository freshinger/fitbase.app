<style>
    .page-header {
        margin-top: 10px !important;
    }

    .container {
        max-width: 100% !important;
        width: 100% !important;
        height: 100%;

    }

    .content {
        padding-bottom: 30px;
        width: 100% !important;
        max-width: 1000px !important;
        margin-left: auto;
        margin-right: auto;
        padding-left: 15px;
        padding-right: 15px;
        min-height: 100%;
    }

    .sonata-bc .page-footer {
        border: none;
        color: #909090;
        background-color: #f5f5f5 !important;
    }

    .page-footer {
        font-size: smaller;
    }

    .page-content > div {
        margin: 10px;
    }

    .page-header > ul {
        margin: 10px;
    }

    html, body {
        height: 90%;
    }


</style>
<div class="col-md-12" style="margin-bottom: 20px; width: 50%;">

    {% set logo = '/image/logo.png' %}

    {% if company is defined and company %}

        {% if company.header is defined and company.header %}
            <style class="company">
                .page-header {
                    background-color: {{ company.header }};
                }
            </style>
        {% endif %}

        {% if company.background1 is defined and company.background1 %}
            <style class="company">
                .page-content {
                    background-color: {{ company.background1 }};
                }
            </style>
        {% endif %}

        {% if company.background2 is defined and company.background2 %}
            <style class="company">
                .sonata-bc {
                    background-color: {{ company.background2 }};
                }
            </style>
        {% endif %}

        {% if company.image is defined and company.image %}
            {% set logo = null %}
            {% thumbnail company.image, 'icon' with { 'width': '60' } %}
        {% endif %}
    {% endif %}

    {% if logo is defined and logo %}
        <img src="{{ logo }}"/>
    {% endif %}

</div>