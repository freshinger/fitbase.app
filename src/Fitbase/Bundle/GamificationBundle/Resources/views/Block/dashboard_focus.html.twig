<div class="panel panel-default">
    <div class="panel-body todays-exercise">

        <h4 class="short hue">
            {{ 'Ihr Fokus'|trans({}, 'FitbaseGamificationBundle') }}
        </h4>

        <div class="row">
            {% if category is defined and category %}
                <div class="col-xs-4" align="center" style="margin-top: 10px; margin-bottom: 15px">
                    <a href="{{ path('focus', {'slug': category.slug}) }}">
                        {% if category.media is defined %}
                            {% thumbnail category.media, 'preview' with {'width': 75} %}
                        {% endif %}
                    </a>
                </div>
            {% endif %}

            <div class="col-xs-8">
                {% if category is defined and category %}
                    <p>Momentan haben Sie den Fokus {{ category.name }}  gewählt.</p>
                {% endif %}

                {% if nextWeeklytask is defined and nextWeeklytask %}
                    <p>Ihre nächste Infoeinheit erhalten Sie in {{ nextWeeklytask }} Tag(en).</p>
                {% endif %}

                {% if nextExercise is defined and nextExercise %}
                    <p>Ihre nächste Aktivität ist geplant in {{ nextExercise }} Stunden.</p>
                {% endif %}

            </div>
            {% if collection is defined and collection %}
                <div class="col-xs-12">
                    <h4>{{ 'Letzte Infoeinheiten'|trans({}, 'FitbaseGamificationBundle') }}</h4>
                    <table class="table table-striped table-hover table-responsive">
                        <tbody>
                        {% for task in collection %}
                            {% if task is defined and task %}
                                <tr>
                                    <td class="shade">
                                        {{ task.task }}

                                        {% if task.task.categories is defined %}
                                            {% for category in task.task.categories %}
                                                {% if category is defined and category %}
                                                    <sub>
                                                        {% if loop.index > 1 %}
                                                            ,
                                                        {% endif %}
                                                        {{ category }}
                                                    </sub>
                                                {% endif %}
                                            {% endfor %}
                                        {% endif %}

                                    </td>
                                    <td class="shade">
                                        <a href="{{ path('weeklytask_user_view', { 'unique': task.id } ) }}">
                                            {{ 'mehr...'|trans({}, 'FitbaseGamificationBundle') }}
                                        </a>
                                    </td>
                                </tr>
                            {% endif %}
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% endif %}
        </div>

    </div>
</div>