<div class="panel panel-default">
    <div class="panel-body todays-exercise">

        <h4 class="short hue">
            {{ 'Ihr Focus'|trans({}, 'FitbaseGamificationBundle') }}
        </h4>

        {% if user.focus is defined and user.focus %}
            {% set category = user.focus %}

            <div class="row">
                <div class="col-md-4">
                    <a href="{{ path('exercises_category', {'unique': category.slug}) }}">
                        {% if category.media is defined %}
                            {% thumbnail category.media, 'preview' with {'width': 100} %}
                        {% endif %}
                    </a>
                </div>
                <div class="col-md-8">
                    <p>Momentan haben Sie den {{ category.name }} Fokus gewählt.</p>

                    {#<p>Ihre nächste Infoeinheit erhalten Sie in XX Tagen.</p>#}

                    {#<p>Ihre nächste Aktivität ist geplant in XX Stunden.</p>#}
                </div>
                {% if collection is defined and collection %}
                    <div class="col-md-12">
                        <h4>{{ 'Letzte Infoeinheiten'|trans({}, 'FitbaseGamificationBundle') }}</h4>
                        <table class="table table-striped table-hover table-responsive">
                            <tbody>
                            {% for task in collection %}
                                {% if task is defined and task %}
                                    <tr>
                                        <td class="shade">
                                            {{ task.task }}
                                        </td>
                                        <td class="shade center">
                                            {{ task.date|date('d.m.Y') }}
                                        </td>
                                        <td class="shade">
                                            <a href="{{ path('weeklytask_user_view', { 'unique': task.id } ) }}">
                                                {{ 'mehr...'|trans({}, 'FitbaseGamificationBundle') }}
                                            </a>
                                        </td>
                                    </tr>
                                {% endif %}
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                {% endif %}
            </div>
        {% endif %}

    </div>
</div>