{% extends 'FitbaseUserBundle:Subscriber:layout.html.twig' %}


{% block content %}

    {{ sonata_block_render_event('breadcrumb', { 'context': 'questionnaire', 'current_uri': app.request.requestUri }) }}

    <div class="panel-info">
        <h2>{{ 'Fokus ändern'|trans({}, 'FitbaseUserBundle') }}</h2>

        <p>
            Sie können die Reihenfolge Ihres persönlichen Online-Coachings verändern indem Sie einen Bereich mit der
            linken Maustaste gedrückt halten und verschieben. Wenn Sie anschließend auf den Button „Speichern und
            Coaching aktivieren“ klicken, startet der auf Platz 1 genannte Bereich.
        </p>

        {% include "FitbaseUserBundle:Block:focus.html.twig" %}

    </div>

{% endblock content %}