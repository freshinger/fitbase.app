<div class="panel panel-default">
    <div class="panel-body">

        <h4 class="short hue">
            {{ 'Aktivität'|trans({}, 'FitbaseUserBundle') }}
        </h4>
        <div class="row">
            <div class="col-xs-9">
                {{ 'Anzahl der Teilnehmer:'|trans({}, 'FitbaseUserBundle') }}
            </div>
            <div class="col-xs-3">
                {{ countUser }}
            </div>
        </div>
        <div class="row">
            <div class="col-xs-9">
                {{ 'Anzahl der Blätter:'|trans({}, 'FitbaseUserBundle') }}
            </div>
            <div class="col-xs-3">
                {{ countField }}
            </div>
        </div>
        <div class="row">
            <div class="col-xs-9">
                {{ 'Absolvierte Quizze:'|trans({}, 'FitbaseUserBundle') }}
            </div>
            <div class="col-xs-3">
                {{ countQuiz }}
            </div>
        </div>
    </div>
</div>